import { defineStore } from 'pinia'
export const useMainClientStore = defineStore("mainClient", {
    state: () => ({
    }),
    getters: {
    },
    actions: {
        init(){
            
        },
        
    }

  
})
